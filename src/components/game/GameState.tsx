//this will contain score and number of turns left

import React, { useState } from 'react';

import '../../styles/game/game.scss';

function GameState(props: any): JSX.Element {

return (
<p>GAME STATE</p>
);
}

export default GameState;